import React from "react";

const Loading = () => {
  return (
    <div className="animate-ping m-6 p-6 bg-blue-600 rounded-full fixed flex justify-center items-center">
      <div>Loading</div>
    </div>
  );
};

export default Loading;
